{
  "name": "element-quiz",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "교과별 던전, 협동 보스 레이드와 참여 보상을 연결한 초등 교실 MMORPG",
  "scripts": {
    "dev": "wrangler pages dev public",
    "test": "node --test",
    "check": "node scripts/check.mjs && node --test",
    "deploy": "wrangler pages deploy public --project-name=element-quiz"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  },
  "engines": {
    "node": ">=20"
  }
}
